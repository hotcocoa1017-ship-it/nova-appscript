name: Dispatch Final QM DB Read Authority V1

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/dispatch-once-qm-db-read-v1-final.yml'

permissions:
  contents: read
  actions: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch canonical deployment
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run deploy-apps-script.yml --repo hotcocoa1017-ship-it/nova-appscript --ref main -f deploy=true
