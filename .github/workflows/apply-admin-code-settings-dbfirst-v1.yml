name: Apply Admin Code Settings DB First V1

on:
  push:
    branches: [main]
    paths:
      - 'scripts/.apply_admin_code_settings_dbfirst_v1_20260908'

permissions:
  contents: write

concurrency:
  group: nova-apps-script-production
  cancel-in-progress: false

env:
  APPS_SCRIPT_DEPLOYMENT_ID: AKfycbyhUrA919fns7Sshld421vtvEeOYEWSfO-QzkTGAu4NkJOv7NJaMu827BpuQwOM1_yf

jobs:
  patch-validate-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Remove obsolete Push patch execution from CI
        run: python3 scripts/patch_deploy_ci_remove_obsolete_push_patch_20260908.py

      - name: Apply admin code settings DB-first patch
        run: python3 scripts/patch_admin_code_settings_dbfirst_v1_20260908.py

      - name: Validate admin code settings DB-first
        run: python3 scripts/validate_admin_code_settings_dbfirst_v1_20260908.py

      - name: Validate whole DB transition
        run: python3 scripts/validate_whole_db_transition_v2_20260908.py

      - name: Validate NOVA release
        run: python3 scripts/validate_nova_release.py

      - name: Validate touched JavaScript and workflow cleanup
        shell: bash
        run: |
          set -euo pipefail
          node --check DbFirstBridge.js
          node --check AdminSettingsDbFirstBridge.js
          node --check AdminCodeSettingsDbFirstBridge.js
          python3 - <<'PY'
          from pathlib import Path
          import re, subprocess
          text=Path('Client.html').read_text(encoding='utf-8')
          blocks=re.findall(r'<script[^>]*>(.*?)</script>',text,flags=re.S|re.I)
          if not blocks: raise SystemExit('Client script block missing')
          subprocess.run(['node','--check','-'],input='\n'.join(blocks),text=True,check=True)
          workflow=Path('.github/workflows/deploy-apps-script.yml').read_text(encoding='utf-8')
          if 'scripts/patch_realtime_subscription_resilience.py' in workflow:
              raise SystemExit('obsolete Push patch execution remains in canonical deploy workflow')
          if 'REALTIME_SUBSCRIPTION_RESILIENCE_V1' not in workflow:
              raise SystemExit('Realtime subscription resilience regression validation was removed')
          PY
          git diff --check

      - name: Persist generated sources and CI cleanup
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add DbFirstBridge.js Client.html .github/workflows/deploy-apps-script.yml scripts/fix_patch_site_scope_v2.py scripts/validate_nova_release.py
          if ! git diff --cached --quiet; then
            git commit -m 'Route admin code settings DB-first and clean obsolete Push CI [skip ci]'
            git push origin HEAD:main
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install clasp
        run: npm install --global @google/clasp

      - name: Configure Apps Script authentication
        shell: bash
        env:
          CLASPRC_JSON: ${{ secrets.CLASPRC_JSON }}
          CLASPRC_JSON_B64: ${{ secrets.CLASPRC_JSON_B64 }}
        run: |
          set -euo pipefail
          if [ -n "${CLASPRC_JSON:-}" ]; then
            printf '%s' "$CLASPRC_JSON" > "$HOME/.clasprc.json"
          elif [ -n "${CLASPRC_JSON_B64:-}" ]; then
            printf '%s' "$CLASPRC_JSON_B64" | base64 --decode > "$HOME/.clasprc.json"
          else
            echo '::error::Apps Script OAuth secret missing'; exit 3
          fi
          chmod 600 "$HOME/.clasprc.json"
          clasp show-authorized-user --json

      - name: Deploy existing NOVA web app
        shell: bash
        run: |
          set -euo pipefail
          clasp push --force
          clasp create-deployment --deploymentId "$APPS_SCRIPT_DEPLOYMENT_ID" --description "Admin code settings DB-first V1 ${GITHUB_SHA::12}"
